{
  "version": 2,
  "buildCommand": "npm --prefix kunuLeft install && npm --prefix kunuLeft run build",
  "outputDirectory": "kunuLeft/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
